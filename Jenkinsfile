pipeline {
    agent  any
    stages {
        stage ("Build Checkout") {
            steps {
                git 'https://github.com/justmorpheus/insecure-python-app.git'

            }
        }
    }
}
